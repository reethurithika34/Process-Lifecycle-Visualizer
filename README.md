# Process-Lifecycle-Visualizer
An interactive web-based simulation tool that visualizes the lifecycle of processes in an operating system. The project demonstrates how processes transition through different states — New, Ready, Running, Waiting, and Terminated — while incorporating key OS concepts such as CPU scheduling, context switching, and resource management
